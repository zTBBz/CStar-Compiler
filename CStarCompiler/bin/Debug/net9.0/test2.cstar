module Vector;

struct Vector : Create<Vector>
{ 
    int x; 
}

void Test()
{
    // Generics + Arrays + Pointers
    Vector[] array = Array.Create<Vector>(128); 
    void* ptr = &array[0];
    
    // Complex math with dereference
    *ptr = vector.x * -10;
    
    // Shortcuts
    SumAndPrint(1, 2);
}
